# ── Local LLM (Ollama) ──────────────────────────────────────────────────────
OLLAMA_URL=http://localhost:11434/api/generate
LOCAL_MODEL=sqlcoder

# ── Databricks (leave blank to use local DuckDB) ────────────────────────────
DATABRICKS_HOST=
DATABRICKS_TOKEN=
DATABRICKS_CATALOG=
DATABRICKS_SCHEMA=

# ── Colombia datos.gov.co (Socrata) ─────────────────────────────────────────
# Optional — removes anonymous rate limits on the SFC Formato 290 download.
# Register free at https://data.socrata.com/profile/app_tokens
SOCRATA_APP_TOKEN=
